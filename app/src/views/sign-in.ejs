<!DOCTYPE html>
<html>
  <%- include('components/head'); -%>
  <body class="app">
    <div class="login-card">
      <h1><%= title %></h1>
      <form action="<%= authServerUrl %>/token" method="post">
        <label>Client Id</label>
        <input required name="client_id" value="<%= clientId %>" />
        <label>Client Secret</label>
        <input required name="client_secret" value="<%= clientSecret %>" />
        <label>Resource</label>
        <input name="resource" value="<%= apiUrl %>/pi" />
        <label>Grant Type</label>
        <input required name="grant_type" value="password" />
        <label>Scope</label>
        <input required name="scope" value="openid offline_access api:read" />
        <label>Prompt</label>
        <input required name="prompt" value="consent" />
        <label>Username</label>
        <input required name="username" value="sample" />
        <label>Password</label>
        <input required type="password" name="password" value="pass" />

        <button type="submit" class="login login-submit">Issue Token</button>
      </form>
    </div>
  </body>
</html>
